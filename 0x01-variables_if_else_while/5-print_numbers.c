#include <stdio.h>

/**
 * main -Enrty point
 *
 * Descreption: prints all single digit numbers of base 10 starting from 0
 *
 *Return: 0 (success)
*/

int main(void)
{
	int num;

	for (num = 0 ; num < 10 ; num++)
	{
		printf("d", nunm);
	}
	printf('\n');

	return (0);
}
